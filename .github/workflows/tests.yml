name: Tests

on:
  pull_request:
  push:
  schedule:
    # Run Test every Saturday at 1000 UTC
    - cron: "0 10 * * 6"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      #----------------------------------------------
      #       check-out repo and set-up python
      #----------------------------------------------
      - name: Check out repository
        uses: actions/checkout@v3
      - name: Set up python
        id: setup-python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      #----------------------------------------------
      #       Load cached venv if cache exists
      #----------------------------------------------
      - name: Load cached Poetry environment
        id: cached-poetry-environment
        uses: actions/cache@v3
        with:
          path: ${{ env.HOME }}/.cache/pypoetry
          key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}
          
      #----------------------------------------------
      # Skip if cache hit, otherwise install Poetry
      #----------------------------------------------
      - name: Install Poetry
        if: steps.cached-poetry-environment.outputs.cache-hit != 'true'
        run: |
          python -m pip install poetry
          poetry config cache-dir ${{ env.HOME }}/.cache/pypoetry
          poetry install --no-root
          
      #----------------------------------------------
      #              Run test suite
      #----------------------------------------------
      - name: Run tests
        run: |
          source ${{ env.HOME }}/.cache/pypoetry/virtualenvs/myenv/bin/activate
          pytest --cov=taruh
